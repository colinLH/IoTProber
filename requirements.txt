# ──────────────────────────────────────────────────────────────────────────────
# IoTProber — Minimal Python Requirements
# Tested environment: probe (Python 3.10 / 3.11, Linux)
#
# PyTorch (CUDA 12.x) — install separately BEFORE this file:
#   pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
#
# PyTorch Geometric — install after torch:
#   pip install torch-geometric==2.7.0
#
# GPU-only extras (bitsandbytes) are commented out below.
# Uncomment on a CUDA Linux host to enable 4-bit QLoRA fine-tuning of LLaMA.
# ──────────────────────────────────────────────────────────────────────────────

# ── Data & ML ─────────────────────────────────────────────────────────────────
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
scipy==1.15.3
hdbscan==0.8.40
joblib==1.5.2
networkx==3.4.2
matplotlib==3.10.7

# ── Deep Learning (see header for torch / torch-geometric install) ─────────────
transformers==4.57.3
accelerate==1.13.0
peft==0.19.0
trl==1.1.0
datasets==4.8.4
sentence-transformers==5.1.2
safetensors==0.7.0
tokenizers==0.22.1
# bitsandbytes==0.49.2   # GPU-only; required for 4-bit QLoRA (load_in_4bit=True)

# ── LangChain ─────────────────────────────────────────────────────────────────
# Pinned to the 1.x line; langchain-classic provides the legacy AgentExecutor /
# create_openai_tools_agent API used in agent/decision.py.
langchain==1.1.0
langchain-core==1.1.0
langchain-community==0.4.1
langchain-classic==1.0.0
langchain-deepseek==1.0.1
langchain-google-genai==3.2.0
langchain-openai==1.1.0
langchain-huggingface==1.1.0
langgraph==1.0.4

# ── LLM SDKs ──────────────────────────────────────────────────────────────────
anthropic==0.86.0
openai==2.8.1
google-genai==1.53.0
tiktoken==0.12.0

# ── Vector DB ─────────────────────────────────────────────────────────────────
pymilvus==2.5.18
milvus-lite==2.5.1

# ── Graph DB ──────────────────────────────────────────────────────────────────
py2neo==2021.2.4

# ── Data Acquisition ──────────────────────────────────────────────────────────
censys-platform==0.12.1
requests==2.32.5
tldextract==5.3.1

# ── Geolocation ───────────────────────────────────────────────────────────────
geopy==2.4.1

# ── Web Server (agent/app.py REST API) ────────────────────────────────────────
flask
flask-cors

# ── PDF Generation ────────────────────────────────────────────────────────────
fpdf2==2.8.7

# ── HuggingFace Hub ───────────────────────────────────────────────────────────
huggingface_hub>=0.34.0,<1.0

# ── Misc ──────────────────────────────────────────────────────────────────────
python-dotenv==1.2.1
pydantic==2.12.5
PyYAML==6.0.3
