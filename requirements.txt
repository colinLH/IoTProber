# ──────────────────────────────────────────────────────────────────────────────
# IoTProber — Minimal verified environment (agent control plane)
#
# Verified on: Python 3.11, Linux + CUDA (H800), elastic_slm environment.
# Scope: agent pipeline — query decomposition, multi-level retrieval,
# LangGraph decision (unseen + drift + Gemini/Claude ReAct branches),
# Flask REST API (agent/app.py).
#
# NOT included (install separately if needed):
#   - torch / CUDA wheels — install BEFORE this file:
#       pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
#   - graph-construction extras (torch-geometric, hdbscan, networkx, matplotlib)
#   - unseen-detector training extras (datasets, trl, bitsandbytes)
#
# NOTE on langgraph: langchain==1.1.0 declares langgraph<1.1.0, but the
# decision graph requires langgraph>=1.1 (add_node(defer=) and
# langgraph.runtime.ExecutionInfo, absent in 1.0.x). A single
# `pip install -r requirements.txt` therefore fails with ResolutionImpossible.
# Install in two steps:
#   grep -v '^langgraph' requirements.txt | grep -v '^#' > /tmp/req-nolanggraph.txt
#   pip install -r /tmp/req-nolanggraph.txt
#   pip install langgraph==1.2.11
# ──────────────────────────────────────────────────────────────────────────────

# ── LangChain 1.x control plane ──────────────────────────────────────────────
langchain==1.1.0
langchain-core==1.6.3
langchain-community==0.4.1
langchain-classic==1.0.0
langchain-deepseek==1.0.1
langchain-google-genai==3.2.0
langchain-openai==1.1.0
langchain-huggingface==1.1.0
langgraph==1.2.11

# ── LLM SDKs ──────────────────────────────────────────────────────────────────
anthropic==0.86.0
openai==2.8.1
google-genai==1.53.0
tiktoken==0.12.0

# ── Web server (agent/app.py REST API) ────────────────────────────────────────
flask==3.1.3
flask-cors==6.0.5
tavily-python==0.8.4

# ── Data & ML ──────────────────────────────────────────────────────────────────
numpy==1.26.4
pandas==3.0.2
scikit-learn==1.7.2
scipy==1.15.3
joblib==1.5.2

# ── Embedding / unseen detector (requires torch, see header) ─────────────────
sentence-transformers==5.1.2
transformers==4.57.6
peft==0.19.1
accelerate==1.13.0

# ── Vector / graph DB clients ─────────────────────────────────────────────────
pymilvus==2.5.18
py2neo==2021.2.4

# ── Misc ───────────────────────────────────────────────────────────────────────
python-dotenv==1.2.1
pydantic==2.12.5
